{% from 'macros/forms.jinja' import label %}
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport"
              content="width=device-width, initial-scale=1.0" />
        <meta name="description"
              content="Browser Stack made with Python." />
        <meta name="keywords"
              content="python, browser, stack, selenium" />
        <title>Browser Stack</title>
        <link rel="manifest"
              href="{{ url_for('static', filename='files/manifest.json') }}" />
        <link rel="stylesheet"
              type="text/css"
              href="{{ url_for('static', filename='styles/index.css') }}" />
        <script defer
                type="module"
                src="{{ url_for('static', filename='scripts/index.js') }}"></script>
    </head>
    <body>
        <header>
            <div class="wrapper">
                {# {% include "components/logo.jinja" %} #}
            </div>
        </header>
        <main>
            <div class="wrapper">
                {% include "forms/browser_stack.jinja" %}
            </div>
        </main>
        <footer>
            <div class="wrapper">
            </div>
        </footer>
    </body>
</html>
